import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();
  // add colors which will be used multiple times throughtout the app
  static const primaryLight = Colors.teal;
  static const primaryDark = Colors.tealAccent;
  static const backgroundLight = Color(0xFFF9F9F9);
  static const backgroundDark = Color(0xFF121212);
  static const cardColor = Color(0xFFE0E0E0);
  static const shadow = Colors.black12;
}
